nodejs:
  build:
    base: [nodejs@14]
    build:
      - npm -v
      - node -v
      # - npm i
    # cache: [node_modules]
    deploy:
      # - package.json
      # - node_modules
      # - ./app/index.js
      - ./app/index-simple.js
      # - config.txt
      # - ./app/replace/
